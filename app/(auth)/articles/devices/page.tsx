import { PageShell } from "@/components/composed";

export default function ArticlesDevicesPage() {
  return (
    <PageShell title="Geräte">
      <p className="text-sm text-muted-foreground">
        Wird in Epic 3 umgesetzt (Story 3.4 — Geräte-Übersicht mit
        Verfügbarkeit & Bestandshinweisen).
      </p>
    </PageShell>
  );
}
