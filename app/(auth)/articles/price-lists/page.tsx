import { PageShell } from "@/components/composed";

export default function ArticlesPriceListsPage() {
  return (
    <PageShell title="Preislisten">
      <p className="text-sm text-muted-foreground">
        Wird in Epic 3 umgesetzt (Story 3.1 — Article Master Data & Price
        Lists).
      </p>
    </PageShell>
  );
}
